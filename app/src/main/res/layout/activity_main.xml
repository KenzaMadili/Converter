<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- Barre d'onglets -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayoutMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        android:contentDescription="Navigation entre les onglets température et distance"
        android:importantForAccessibility="yes"
        app:tabIndicatorColor="#3F51B5"
        app:tabSelectedTextColor="#3F51B5"
        app:tabTextColor="#616161"
        app:tabIndicatorFullWidth="false"
        app:tabMode="fixed"
        app:tabGravity="fill"/>

    <!-- Conteneur des fragments -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerMain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:contentDescription="Zone d'affichage des contenus des onglets"/>

</LinearLayout>